# Post-044 scripts
- These scripts are made with a new method, instead of retranslating / reusing some conventions from the original PC patch,
it's the full lines from the PC patch, with some light editing and only translating lines that did not have matching ones in the original PC patch's lines
- These are made using a line injector script, made to serve the kr2 -> SC3 copy-over.
- The packages include the "cooked" (ready-to-use) scripts at the sources (src) text files.
- These will also be uploaded when they're cleaned and verified to work.
